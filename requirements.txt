# Direct script dependencies
PyYAML
matplotlib
Pillow
datasets
math-verify

# Vendored Transformers runtime dependencies
huggingface-hub
tokenizers==0.22.2
safetensors
accelerate
numpy
packaging
filelock
regex
requests
tqdm

# Install torch separately according to the CUDA image.
